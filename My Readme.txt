https://github.com/ndepaola/mtg-autoproxy

1. Can only proxy one card at a time since it seems the card.json does not get replaced at all. Need to manually run the script below for each card.
py get_card_info.py "Chandra's Spitfire"